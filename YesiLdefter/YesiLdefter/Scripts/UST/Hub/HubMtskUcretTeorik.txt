/*CreateTable*/

begin transaction

 create table HubMtskUcretTeorik
 (

   Id                     Int IDENTITY(1,1) NOT NULL, 
   FirmId                 Int        not null,
   IsActive               Bit        not null, /* default : 1 = active, 0 = not active */  
   IlKodu                 Varchar(3) not null, /* il bazlı */  
   GecerlilikTarihiId     Int        not null,

   /* Ücretler */  
   BirSaatTeorikDersUcreti    Numeric(22,5) null,  

  constraint pk_HubMtskUcretTeorik primary key (Id)
 )

 create index X_HubMtskUcretTeorik01 on HubMtskUcretTeorik (IlKodu, GecerlilikTarihiId)
 
 grant select, insert, update, delete on HubMtskUcretTeorik to public

commit transaction

/*CreateTableEnd*/
