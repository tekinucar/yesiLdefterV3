/*CreateTable*/

begin transaction

 create table SrcUcretTeorik
 (

   Id                     Int IDENTITY(1,1) NOT NULL, 
   FirmId                 Int        not null,
   IsActive               Bit        not null, /* default : 1 = active, 0 = not active */  
   IlKodu                 Varchar(3) not null, /* il bazlı */  
   GecerlilikTarihiId     Int        not null,

   /* Ücretler */  
   BirSaatTeorikDersUcreti    Numeric(22,5) null,  

  constraint pk_SrcUcretTeorik primary key (Id)
 )

 create index X_SrcUcretTeorik01 on SrcUcretTeorik (IlKodu, GecerlilikTarihiId)
 
 grant select, insert, update, delete on SrcUcretTeorik to public

commit transaction

/*CreateTableEnd*/
